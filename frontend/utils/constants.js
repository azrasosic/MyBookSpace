/**
 * Application Constants
 *
 * Defines global configuration constants including API endpoints,
 * project base URL, and user role identifiers for the library management system.
 */
window.Constants = window.Constants || {
  PROJECT_BASE_URL: 'http://localhost/MyBookSpace/backend/',
  API_ENDPOINTS: {
    LOGIN: 'auth/login',
  },
  USER_ROLE: 'user',
  LIBRARIAN_ROLE: 'librarian',
};